{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && daphne talentco.asgi:application --port $PORT --bind 0.0.0.0",
    "healthcheckPath": "/api/v1/health/"
  }
} 